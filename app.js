const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);
const toast=t=>{const el=$('#toast');el.textContent=t;el.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>el.classList.remove('show'),1800)};
const titles={general:'The main transmission channel',showcase:'Show what you are building',builds:'Build logs and experiments',random:'Everything else'};
$$('.channel').forEach(btn=>btn.addEventListener('click',()=>{ $$('.channel').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const name=btn.dataset.channel;$('#channelTitle').textContent=name;$('#channelSubtitle').textContent=titles[name]||'Community channel';$('#messageInput').placeholder=`Message #${name}`;toast(`Switched to #${name}`)}));
$$('.server').forEach(btn=>btn.addEventListener('click',()=>{ $$('.server').forEach(x=>x.classList.remove('active'));btn.classList.add('active');toast(`${btn.dataset.server} network selected`)}));
$$('.reaction-row button:not(.add-reaction)').forEach(btn=>btn.addEventListener('click',()=>{const m=btn.textContent.match(/(\d+)$/);if(m)btn.textContent=btn.textContent.replace(/\d+$/,String(Number(m[1])+1));btn.animate([{transform:'scale(1)'},{transform:'scale(1.12)'},{transform:'scale(1)'}],{duration:220});}));
$$('.add-reaction').forEach(btn=>btn.addEventListener('click',()=>{btn.textContent='⚡';btn.classList.remove('add-reaction');toast('Reaction added')}));
$$('.voice-call button').forEach(btn=>btn.addEventListener('click',()=>{btn.textContent='Joined';btn.disabled=true;toast('Joined Focus Room')}));
$('#themeBtn').addEventListener('click',()=>{document.body.classList.toggle('warm');toast(document.body.classList.contains('warm')?'Cyan signal enabled':'Violet signal enabled')});
$('#messageForm').addEventListener('submit',e=>{e.preventDefault();const input=$('#messageInput');const text=input.value.trim();if(!text)return;const article=document.createElement('article');article.className='message';article.innerHTML=`<div class="message-row"><div class="avatar user-avatar">S<span></span></div><div class="message-body"><div class="meta"><strong>Operator</strong><span>just now</span></div><p></p><div class="reaction-row"><button>⚡ 0</button><button>❤️ 0</button><button class="add-reaction">+</button></div></div></div>`;article.querySelector('p').textContent=text;$('#feed').appendChild(article);article.scrollIntoView({behavior:'smooth',block:'end'});input.value='';toast('Transmission sent')});
$('#searchInput').addEventListener('input',e=>{const q=e.target.value.toLowerCase().trim();$$('.message').forEach(m=>m.style.display=!q||m.textContent.toLowerCase().includes(q)?'':'none')});
document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();$('#searchInput').focus()}});
$('#addChannel').addEventListener('click',()=>{const name=prompt('New channel name:');if(!name)return;const clean=name.toLowerCase().replace(/[^a-z0-9-]/g,'-').slice(0,24);if(!clean)return;const btn=document.createElement('button');btn.className='channel';btn.dataset.channel=clean;btn.innerHTML=`<span>#</span> ${clean}`;btn.addEventListener('click',()=>{ $$('.channel').forEach(x=>x.classList.remove('active'));btn.classList.add('active');$('#channelTitle').textContent=clean;$('#channelSubtitle').textContent='New NEXUS channel';$('#messageInput').placeholder=`Message #${clean}`});$('.nav-group:nth-child(2)').appendChild(btn);toast(`#${clean} created`)});
setTimeout(()=>toast('NEXUS online · signal stable'),500);